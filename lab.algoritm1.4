#include <iostream>
#include <cmath>
using namespace std;
int main() {
double a, b, c, x1,x2,d;
  cout <<"Введите коэффициенты уравнения"<< "\n";
  cin >> a;
  cin >> b;
  cin >> c;

  if (a==0) {
    if (b==0) { if (c==0)
    cout <<"Множество корней";
    if (c!=0)
    cout <<"Нет корней"; 
    };
    if (b!=0)
    {x1=(-c/b);
    cout << x1;};
    };
  if (a!=0) {
     if (b==0) {if (c==0) {x1=0; cout << x1;};} ;
     if (b==0) {if (c!=0) {x1=sqrt(-c/a); x2=-sqrt(-c/a); cout << x1 << " " << x2;};} ;
     if (b!=0) {if (c==0) {x1=0; x2=(-b/a); cout << x1<< " " << x2;};} ;
     if (b!=0) {if (c!=0) { d=b*b-4*a*c; 
       if (d<0) {cout <<  "Не имеет рациональных корней ";};
       if (d>=0) {
         x1=(-b+sqrt(d))/(2*a); 
         x2=(-b-sqrt(d))/(2*a);
         cout <<x1 << " " << x2; }
       } ;     
    }
  }
}
