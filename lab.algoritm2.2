#include <iostream>
#include <cmath>
using namespace std;
int main() 
{
  double n, s, k, d;
  cout << "Введите число : \n";
  cin >> n;
  k=10000;
  s=101;
  for ( s=101; s<=n; s++) 
  {
   k=k+(s*s);
   d++;
  }
  d=d+1;
  k=k/(d);
  cout << k;
}
